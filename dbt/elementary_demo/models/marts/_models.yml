version: 2

models:
  - name: customers
    description: '{{ doc("customers") }}'
    columns:
      - name: customer_id
        description: Unique customer identifier.
        tests:
          - unique
          - not_null
